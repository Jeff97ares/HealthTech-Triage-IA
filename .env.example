# Este archivo es solo referencia general del proyecto.
# Las variables reales se configuran en:
#   backend/.env   (ver backend/.env.example)
#   frontend/.env  (ver frontend/.env.example)
#
# Resumen de las variables del backend relacionadas con la IA:
#   AI_ENABLED=true|false
#   OPENAI_API_KEY=coloque_aqui_su_clave
#   OPENAI_MODEL=modelo_configurable
#   AI_TIMEOUT_SECONDS=8
#   DATABASE_URL=postgresql://usuario:contraseña@127.0.0.1:5434/healthtech_triage
#
# Nunca subas un archivo .env real (con claves o contraseñas) a GitHub.
